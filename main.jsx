import React from "react";
import ReactDOM from "react-dom/client";
import TennisReachVisualizer from "./TennisReachVisualizer.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TennisReachVisualizer />
  </React.StrictMode>
);
